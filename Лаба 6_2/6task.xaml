<Window x:Class="Лаба_6_2._6task"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Лаба_6_2"
        mc:Ignorable="d"
        Title="_6task" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Введите первый элемент прогресии введите элемент прогрессии и его знаменатель" FontSize="14" FontWeight="Bold" FontFamily="Consolas" VerticalContentAlignment="Center" />
        <TextBox x:Name="first" Grid.Row="1"/>
        <TextBox x:Name="num" Grid.Row="2"/>
        <TextBox x:Name="second" Grid.Row="3"/>
        <Button Click="elementj" Grid.Row="4" Content="Посчитать элемент прогресии" FontSize="14" FontFamily="Consolas" VerticalContentAlignment="Stretch"/>
        <TextBlock x:Name="result" Grid.Row="5" FontSize="14" FontFamily="Consolas" />
        <Button Grid.Row="7" Content="Назад в меню" Click="GoBack_Click" 
                FontSize="14" FontFamily="Consolas" Margin="5" Padding="10,5"/>
        <TextBlock Grid.Row="8" Text="Элемент aj геометрической прогрессии вычисляется
по формуле aj= a0*r, j = 0,1,2, Поле first – вещественное число, первый
элемент прогрессии a0; поле second – знаменатель прогрессии, r. Определить
метод elementj() –для вычисления заданного элемента прогрессии"/>
    </Grid>
</Window>
